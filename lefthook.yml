pre-commit:
  commands:
    check:
      glob: "*.{ts,tsx,json,.md,.css,.scss}"
      run: pnpm exec @biomejs/biome check --no-errors-on-unmatched --files-ignore-unknown=true --colors=off {staged_files}