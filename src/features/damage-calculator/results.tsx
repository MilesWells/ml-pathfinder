'use client';

import { Fieldset, Title } from '@mantine/core';

export function Results() {
	return (
		<Fieldset legend={<Title order={3}>Results</Title>} mx="auto" w="fit-content">
			Results
		</Fieldset>
	);
}
