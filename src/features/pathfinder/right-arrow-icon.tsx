import { Center } from '@mantine/core';

export function RightArrowIcon() {
	return (
		<Center
			styles={{
				root: {
					cursor: 'default',
					fontSize: 22,
					height: 30,
					width: 30,
				},
			}}
		>
			➡️
		</Center>
	);
}
