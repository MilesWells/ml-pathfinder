import { Center } from '@mantine/core';

export function TaxiIcon() {
	return (
		<Center
			styles={{
				root: {
					cursor: 'default',
					fontSize: 22,
					height: 30,
					width: 30,
				},
			}}
			title="Taxi"
		>
			🚕
		</Center>
	);
}
