import { Center } from '@mantine/core';

export function WalkIcon() {
	return (
		<Center
			styles={{
				root: {
					cursor: 'default',
					fontSize: 26,
					height: 30,
					width: 30,
				},
			}}
			title="Walk"
		>
			🚶
		</Center>
	);
}
