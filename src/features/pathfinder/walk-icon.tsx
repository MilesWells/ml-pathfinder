import { Center } from '@mantine/core';

export function WalkIcon() {
	return (
		<Center
			styles={{
				root: {
					fontSize: 24,
					height: 30,
					width: 30,
				},
			}}
		>
			🚶
		</Center>
	);
}
